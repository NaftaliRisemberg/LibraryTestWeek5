@model IEnumerable<LibraryTestWeek5.Models.Library>

@{
    ViewData["Title"] = "Library";
}

<h1>ספריות</h1>

<p>
    <a asp-action="Create"> הוסף ז'אנר</a>
</p>
<table class="table col-sm-4">
    <thead>
        <tr>
            <th>
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre)
            </td>
            <td>
                @Html.ActionLink("עריכה", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("מחיקה", "Delete", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("הוסף מדף", "AddShelf", new { id = item.Id }) |
                @Html.ActionLink(" הצג מדפים", "Shelves", new { id = item.Id })
            </td>
        </tr>
}
    </tbody>
</table>
